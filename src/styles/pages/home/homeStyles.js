import { makeStyles } from "@material-ui/core"

const homeStyle = makeStyles(() => ({
  root: {
    background: "#424242",
    paddingBottom: "60px",
    minHeight: "100vh",
  },
}))

export default homeStyle

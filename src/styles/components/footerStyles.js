import { makeStyles } from "@material-ui/core"

const footerStyles = makeStyles(() => ({
  appBar: {
    position: "relative",
    top: "auto",
    bottom: 0,
  },
  grow: {
    flexGrow: 1,
  },
}))

export default footerStyles
